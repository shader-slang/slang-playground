{
    "name": "slang-playground-engine",
    "version": "0.0.1",
    "workspaces": ["*"],
    "scripts": {
        "build": "npm run build -w shared && npm run build --workspaces --exclude=shared"
    }
}